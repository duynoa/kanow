import LayoutVehicleManagement from '@/components/layout/LayoutVehicleManagement'
import LayoutVehicleManagementMobile from '@/components/layout/LayoutVehicleManagementMobile'
import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Kanow - Quản lý xe',
    description: 'Generated by create next app',
}

export default function RootLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <LayoutVehicleManagementMobile>
            {children}
        </LayoutVehicleManagementMobile>
    )
}
